Program:
Inputs: %M %N %T_subtract %A %B 
  Branch: %entry
  True Ratio: TR_%entry
  True Side Graph:
    BasicBlock: %for_begin_ax1.preheader.us.preheader
    Count: scMul(TR_%entry, 1)
    Loop: %for_begin_ax1.preheader.us
    Loop Count: scAdd(scAdd(-1, scZeroExt(%M)), 1)
    Head Graph:
      Branch: %for_begin_ax1.preheader.us
      True Ratio: TR_%for_begin_ax1.preheader.us
      True Side Graph:
        BasicBlock: %for_body_ax1.us.preheader
        Count: scMul(TR_%for_begin_ax1.preheader.us, scMul(scAdd(scAdd(-1, scZeroExt(%M)), 1), scMul(TR_%entry, 1)))
        Loop: %for_body_ax1.us
        Loop Count: scAdd(scAdd(-1, scZeroExt(%N), scMul(-1, %indvars.iv.ph)), 1)
        Head Graph:
          BasicBlock: %for_body_ax1.us
          Count: scMul(scAdd(scAdd(-1, scZeroExt(%N), scMul(-1, %indvars.iv.ph)), 1), scMul(TR_%for_begin_ax1.preheader.us, scMul(scAdd(scAdd(-1, scZeroExt(%M)), 1), scMul(TR_%entry, 1))))
        Body Graph:
          Graph is null
        Graph is null
      False Side Graph:
        Loop: %vector.body
        Loop Count: scAdd(scUDiv(scAdd(-8, scMul(8, scZeroExt(scTrunc(scUDiv(scZeroExt(%N), 8))))), 8), 1)
        Head Graph:
          BasicBlock: %vector.body
          Count: scMul(scAdd(scUDiv(scAdd(-8, scMul(8, scZeroExt(scTrunc(scUDiv(scZeroExt(%N), 8))))), 8), 1), scMul(scSub(1, TR_%for_begin_ax1.preheader.us), scMul(scAdd(scAdd(-1, scZeroExt(%M)), 1), scMul(TR_%entry, 1))))
        Body Graph:
          Graph is null
        Branch: %middle.block
        True Ratio: TR_%middle.block
        True Side Graph:
          Graph is null
        False Side Graph:
          BasicBlock: %for_body_ax1.us.preheader
          Count: scMul(scSub(1, TR_%middle.block), scMul(scSub(1, TR_%for_begin_ax1.preheader.us), scMul(scAdd(scAdd(-1, scZeroExt(%M)), 1), scMul(TR_%entry, 1))))
          Loop: %for_body_ax1.us
          Loop Count: scAdd(scAdd(-1, scZeroExt(%N), scMul(-1, %indvars.iv.ph)), 1)
          Head Graph:
            BasicBlock: %for_body_ax1.us
            Count: scMul(scAdd(scAdd(-1, scZeroExt(%N), scMul(-1, %indvars.iv.ph)), 1), scMul(scSub(1, TR_%middle.block), scMul(scSub(1, TR_%for_begin_ax1.preheader.us), scMul(scAdd(scAdd(-1, scZeroExt(%M)), 1), scMul(TR_%entry, 1)))))
          Body Graph:
            Graph is null
          Graph is null
        Graph is null
    Body Graph:
      BasicBlock: %for_begin_ax1.for_end_ax1_crit_edge.us
      Count: scMul(scAdd(scAdd(-1, scZeroExt(%M)), 1), scMul(TR_%entry, 1))
      Graph is null
    Graph is null
  False Side Graph:
    Graph is null
  BasicBlock: %for_end_ax0
  Count: 1
  Graph is null
