Program:
Inputs: %M %N %T_concat %B %A 
  Branch: %entry
  True Ratio: TR_%entry
  True Side Graph:
    Branch: %for_begin_ax1.preheader.us.preheader
    True Ratio: TR_%for_begin_ax1.preheader.us.preheader
    True Side Graph:
      Graph is null
    False Side Graph:
      BasicBlock: %for_begin_ax1.preheader.us.preheader.new
      Count: scMul(scSub(1, TR_%for_begin_ax1.preheader.us.preheader), scMul(TR_%entry, 1))
      Loop: %for_begin_ax1.preheader.us
      Loop Count: scAdd(scUDiv(scAdd(-2, scMul(2, scUDiv(scZeroExt(scSMax(1, scMul(2, %M))), 2))), 2), 1)
      Head Graph:
        BasicBlock: %for_begin_ax1.preheader.us
        Count: scMul(scAdd(scUDiv(scAdd(-2, scMul(2, scUDiv(scZeroExt(scSMax(1, scMul(2, %M))), 2))), 2), 1), scMul(scSub(1, TR_%for_begin_ax1.preheader.us.preheader), scMul(TR_%entry, 1)))
      Body Graph:
        Graph is null
      Graph is null
    Branch: %for_end_ax0.loopexit.unr-lcssa
    True Ratio: TR_%for_end_ax0.loopexit.unr-lcssa
    True Side Graph:
      Graph is null
    False Side Graph:
      BasicBlock: %for_begin_ax1.preheader.us.epil
      Count: scMul(scSub(1, TR_%for_end_ax0.loopexit.unr-lcssa), scMul(TR_%entry, 1))
      Graph is null
    Graph is null
  False Side Graph:
    Graph is null
  BasicBlock: %for_end_ax0
  Count: 1
  Graph is null
